---
title: 共形映射及其应用 (Conformal Mapping)
description: 探索复变函数的几何意义：保持角度不变的变换及其在物理中的应用
---

import KnowledgeCard from "@site/src/components/KnowledgeCard";
import { motion } from "framer-motion";
import { Compass, Maximize, Move } from "lucide-react";

# 共形映射及其应用

共形映射（保角映射）是解析函数的几何表现。它将一个复杂的区域映射到一个简单的区域（如单位圆或上半平面），从而简化物理问题的求解。

---

## 一、共形映射的定义

### 1. 几何特性

设 $w = f(z)$ 在 $z_0$ 处解析且 $f'(z_0) \neq 0$：

- **保角性**：经过 $z_0$ 的两条曲线，其映射后的像曲线之间的夹角（大小和方向）保持不变。
- **伸缩率不变性**：在 $z_0$ 的微小邻域内，所有方向的伸缩比例均为 $|f'(z_0)|$。

<KnowledgeCard type="info" title="共形 (Conformal) 的充要条件">
若 $f(z)$ 在区域 $D$ 内单叶（一一对应）且解析，则 $f(z)$ 是 $D$ 上的共形映射。
</KnowledgeCard>

---

## 二、分式线性变换 (Möbius Transformation)

$$ w = \frac{az + b}{cz + d}, \quad ad - bc \neq 0 $$

### 1. 核心性质

- **保圆性**：将复平面上的圆（包括直线，视为半径无穷大的圆）映射为圆。
- **保交比性**：四个点的交比在变换下保持不变。
- **对称点映射**：将关于圆对称的点映射为关于像圆对称的点。

### 2. 三点决定唯一变换

给定 $z$ 平面上的三个不同点 $z_1, z_2, z_3$ 及其像点 $w_1, w_2, w_3$，可以唯一确定一个分式线性变换。

---

## 三、典型区域映射

| 函数类型                     | 映射效果                  | 典型应用           |
| :--------------------------- | :------------------------ | :----------------- |
| **指数函数** $e^z$           | 带状区域 $\to$ 角形区域   | 求解平行板间的电势 |
| **幂函数** $z^n$             | 角形区域 $\to$ 半平面     | 尖角附近的流场分析 |
| **儒可夫斯基变换** $z + 1/z$ | 圆外区域 $\to$ 翼型外区域 | 航空动力学理论基础 |

---

## 🎯 经典练习

### 练习 1：求分式线性变换

求将上半平面 $\text{Im}\,z > 0$ 映射为单位圆 $|w| < 1$，且满足 $f(i) = 0$ 的变换。

<details>
<summary>点击查看解析</summary>

根据对称点性质，$z = i$ 的像点为 $w = 0$，则其关于实轴的对称点 $z = -i$ 的像点应为 $w = \infty$。
故变换形式为：
$$ w = k \frac{z - i}{z + i} $$
由于边界映射到边界（实轴映射到单位圆），取 $z = 0$（在实轴上），则 $|w| = 1$：
$$ |k \frac{0 - i}{0 + i}| = |k| \cdot 1 = 1 \implies |k| = 1 $$
故满足条件的变换为 $w = e^{i\theta} \frac{z - i}{z + i}$。

</details>

### 练习 2：判定映射区域

函数 $w = z^2$ 将第一象限 $0 < \arg z < \pi/2$ 映射到哪里？

<details>
<summary>点击查看解析</summary>

令 $z = re^{i\theta}$，则 $w = r^2 e^{i2\theta}$。
当 $0 < \theta < \pi/2$ 时，$0 < 2\theta < \pi$。
故第一象限被映射到 **上半平面** $\text{Im}\,w > 0$。

</details>
