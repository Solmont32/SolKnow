# 函数基础 (Basic Functions)

## 定义
给定两个变量 $x, y$，如果对于 $x$ 的每一个值，$y$ 都有唯一确定的值与之对应，则称 $y$ 是 $x$ 的函数。

## 正比例函数
$y = kx$ ($k \neq 0$)，图象是过原点的直线。

## 一次函数
$y = kx + b$ ($k \neq 0$)，图象是一条直线。$k$ 决定斜率，$b$ 决定 $y$ 轴截距。

## 反比例函数
$y = \frac{k}{x}$ ($k \neq 0$)，图象是双曲线。
