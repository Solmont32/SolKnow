# 算术 (Arithmetic)

## 四则运算
- **加法 (+)**：将两个数合并。
- **减法 (-)**：找出一个数比另一个数多多少。
- **乘法 (×)**：重复相加。
- **除法 (÷)**：将一个数平均分配。

## 整数性质
- **奇数与偶数**：能被2整除的数是偶数，否则是奇数。
- **质数与合数**：只有1和它本身两个因数的数是质数。

## 运算法则
- **交换律**：$a+b=b+a$, $a \times b = b \times a$
- **结合律**：$(a+b)+c=a+(b+c)$, $(a \times b) \times c = a \times (b \times c)$
- **分配律**：$a \times (b+c) = a \times b + a \times c$
